"""App configuration for conflict_detector."""

from django.apps import AppConfig


class ConflictDetectorConfig(AppConfig):
    """Configuration for conflict_detector app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.conflict_detector"

